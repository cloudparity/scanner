# Everything the image does not need. Keeps the build context small and stops local state - a scan
# output, a kubeconfig, an .env - from being copied into a layer.
.git
.github
docs
out
dist
node_modules
*.md
Dockerfile
.dockerignore
